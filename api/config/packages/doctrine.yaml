doctrine:
    dbal:
        url: '%env(resolve:DATABASE_URL)%'

        # IMPORTANT: You MUST configure your server version,
        # either here or in the DATABASE_URL env var (see .env file)
        #server_version: '16'

        profiling_collect_backtrace: '%kernel.debug%'

        # Messenger's Doctrine transport table (messenger_messages) is
        # provisioned by a migration with auto_setup disabled, so the ORM
        # never learns about it. Exclude it from schema introspection or
        # doctrine:schema:validate / migrations:diff would flag it as an
        # untracked table that schema:update would drop. Same treatment for
        # feedback_ticket_seq: a standalone Postgres sequence (created by a
        # migration, read via nextval in App\State\FeedbackOwnerProcessor)
        # that no entity's ID generator owns, so the ORM never tracks it.
        schema_filter: ~^(?!messenger_messages|feedback_ticket_seq)~

        # doctrine/dbal 4 dropped the legacy `array` type. Gedmo's
        # AbstractLogEntry still declares its `data` column as
        # `array`, so we re-register it as a JSON-backed alias.
        # JsonType encodes/decodes arrays cleanly — exactly what the
        # Loggable listener needs to serialise change sets.
        types:
            array: Doctrine\DBAL\Types\JsonType
    orm:
        # PHP 8.4 native lazy objects replace the old proxy-generation pipeline,
        # so doctrine-bundle 3.x dropped auto_generate_proxy_classes / proxy_dir
        # along with enable_lazy_ghost_objects and report_fields_where_declared
        # (the last two are always-on now).
        enable_native_lazy_objects: true
        controller_resolver:
            auto_mapping: false
        entity_managers:
            default:
                validate_xml_mapping: true
                naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
                identity_generation_preferences:
                    Doctrine\DBAL\Platforms\PostgreSQLPlatform: identity
                auto_mapping: true
                mappings:
                    App:
                        type: attribute
                        is_bundle: false
                        dir: '%kernel.project_dir%/src/Entity'
                        prefix: 'App\Entity'
                        alias: App
                # Postgres-specific full-text search wrappers used by
                # App\Filter\TaskSearchFilter. Registered as string functions
                # because they return scalar values (boolean / float) that DQL
                # treats as string-position expressions in WHERE/ORDER BY.
                dql:
                    string_functions:
                        SEARCH_VECTOR_MATCH: App\Doctrine\Functions\SearchVectorMatch
                        SEARCH_VECTOR_RANK: App\Doctrine\Functions\SearchVectorRank

when@test:
    doctrine:
        dbal:
            # "TEST_TOKEN" is typically set by ParaTest
            dbname_suffix: '_test%env(default::TEST_TOKEN)%'

when@prod:
    doctrine:
        orm:
            entity_managers:
                default:
                    query_cache_driver:
                        type: pool
                        pool: doctrine.system_cache_pool
                    result_cache_driver:
                        type: pool
                        pool: doctrine.result_cache_pool

    framework:
        cache:
            pools:
                doctrine.result_cache_pool:
                    adapter: cache.app
                doctrine.system_cache_pool:
                    adapter: cache.system
